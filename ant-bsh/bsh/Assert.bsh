Assert() {
    notNull(String str) {
        print("Checking [" + str + "]");

        if (null == str) {
            throw new IllegalArgumentException("STR [" + str + "]");
        }
    }

    return this;
}
